package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Clean Minimalism Color Theme
val MinimalBg = Color(0xFFFDFBFF)         // Pure bright light canvas
val MinimalTextPrimary = Color(0xFF1C1B1F) // Deep elegant charcoal for headings
val MinimalTextSecondary = Color(0xFF74777F) // Soft gray for labels and details
val MinimalPrimary = Color(0xFF0061A4)    // Accent deep blue
val MinimalPrimaryLight = Color(0xFFDDE1FF) // Active tab light blue

val CardUpbitBg = Color(0xFFE0E2ED)       // Upbit Card Soft Blueish Gray
val CardStandardBg = Color(0xFFFFFFFF)    // Secondary Card surfaces
val BorderMinimal = Color(0xFFE1E2EC)     // Light minimal separator lines

// Precise Trading Colors
val MinimalTradingRise = Color(0xFFBA1A1A)  // Premium alert red for uptrends
val MinimalTradingFall = Color(0xFF0061A4)  // Accent deep blue for downtrends
val KimchiGoldAccent = Color(0xFF8B1A00)    // Warm crimson/orange accent

// Mapping of legacy color names to Clean Minimalism Light palette values
val ObsidianBg = MinimalBg
val SurfaceCard = CardStandardBg
val BorderGrey = BorderMinimal
val GoldPrimary = MinimalPrimary
val GoldSecondary = MinimalPrimaryLight
val PremiumGold = KimchiGoldAccent
val TextWhite = MinimalTextPrimary
val TextGrey = MinimalTextSecondary
val TradingRise = MinimalTradingRise
val TradingFall = MinimalTradingFall
